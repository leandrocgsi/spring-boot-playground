language: java
jdk:
  - oraclejdk11
before_install:
  # Build Maven Project
  - sudo apt-get update  
  - cd 18_DeployInAWS/RestWithSpringBootUdemy/
  - echo "Let's start Maven Package!"
  - mvn clean package
  - cd ..
services:
  - docker
deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: master
notifications:
  email: false
